{
  "name": "tennis-live-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Lokální tenisový dashboard pro French Open, ATP ranking, H2H a testovací predikce.",
  "scripts": {
    "start": "node server.js",
    "cf:check": "node node_modules/wrangler/bin/wrangler.js deploy --dry-run",
    "cf:deploy": "node node_modules/wrangler/bin/wrangler.js deploy"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "wrangler": "^4.80.0"
  }
}
